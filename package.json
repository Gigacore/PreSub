{
  "name": "metadata-anonymity-checker-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:desktop": "pnpm --filter desktop dev",
    "build": "tsc -b",
    "dist:desktop": "pnpm --filter desktop dist",
    "dist:cli": "pnpm --filter cli dist",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
